<웹 페이지 만들기>
웹 문서의 기본을 만들어보자

책은 2010 버전으로 만들어져 있기 때문에 좋지 않음
책을 산 사람들도 다른 책의 잘 되어진 부분을 가져올 것임

UI와 로직의 부분으로 나뉘어져 있음(비하인드 코드)

week2 -> 추가 -> 새항목 -> visual c# -> 웹 -> web forms -> web form 으로 만듬

IIS에 대한 부분은 실행되고 있음

web.config 파일
config는 3가지 형태로 만들어짐
웹 세팅(시스템 웹, 컴파일러에 대한 부분, system.web)
어플리케이션에 대한 부분
DB 연동에 관한 부분
컴파일러에 대한 부분도 있음...

HTML이란?
pair programming이란 형태로 이루어져 있음 - 해당 부분 코드 입력하면 만들어짐<a>와 </a>, 닫아주는 부분이 발생함
<b>진하게 보기</b>
<!-- 주석처리 -->

week2 -> 추가 -> 새항목 -> visual c# -> 웹 -> html 페이지

&nbsp; 가 공백임

블록 레벨 엘리먼트
다른 줄에 표현되는 엘리먼트
h1, hr, p, pre...

인라인 엘리먼트
같은 줄에 표현되는 엘리먼트
a, b, i, img, font, input.......

경로(path)
내용이 웹 브라우저에 어떻게 보일지를 결정
내용을 포함하고 있는 태그들의 순차적 목록

속성은 name='value'의 형태로 쌍으로 표현
속성 값은 작은 따음표나 큰 따음표 안에 표현
HTML4 명세에 속성 이름 및 속성 값으로 소문자를 사용할 것을 권장

컨트롤의 사용은 id로 한다?

표준속성
class 속성 - 엘리먼트들이 속해 있는 클래스 툴이나 스타일 명시
id 속성 - 엘리먼트들을 식별하기 위해 문서 내에서 유일한 id 부여
style 속성 - 스타일 정의
title 속성 - 툴 팁으로 보이게 될 텍스트 정의


Defalut.html 형태로 만들면 <script> 형태를 넣어서 값을 계산해야 한다. - 좋지 못함
.aspx 형태는 ui는 따로 만들고 로직은 비하인드 코드로 분리해서 만든다

속성의 번개 표시를 누르면 이벤트


디버거
디버그 -> 에서 사용
한줄씩, 중단점 등 다양한 기능이 있음

f11을 이용해서 하나씩 하나씩 가능합니다

2장이 종료 -> 내일은 3장을 하겠습니다.