<ASP.NET 웹페이지 기본>

알아 두어야 할 C#의 기본적인 내용들
값을 넘기는 것은... 파라매터를 넣어주는 부분이 있는 방법,(GET, POST)
이 자체는 POST이지만 GET도 사용 가능합니다

기본 내용들
대소문자의 구분 - 예약어, 변수 이름, 메소드 이름 등에서 대소문자 구분합니다
변수의 이름은 변수의 이름을 보고 어떤 의미의 변수인지 알 수 있게끔 만들어줘야 합니다

세미클론으로 문장의 끝을 나타냄

{}으로 묶어 한꺼번에 처리해야 할 것을 처리함

책은 2010이지만 2019 버전으므로 2019 버전으로 컨버트해서 만듭시다

C#의 변수 이름 명명 규칙 카멜스타일! - memberAge

데이터형은 값과 참조형이 있음
값 - 데이터를 넣음
참조 - 포인터와 같은 개념임, 실제 값이 아닌 값이 들어 있는 주소 번지

값형 - int, float와 사용자가 정의하는 구조체, 즉 user-defined type이 있음(struct, enum) -> 구조체가 발전하여 클래스가 됨

조각프로그램은 추가 - 클래스도 가능하고, 만들어서 뜯어내도 됨!

decimal은 실수형이고 8비트인 double보다 긴 16비트의 형태임

PPT에서는 같은 파일에 enum과 사용을 했지만 코드가 길어지므로 뜯어냅시다

구조체 - 여러 데이터형으로 이루어진 데이터들을 한로 묶어 관리


참조형 - 문자열, 배열

이스케이프 문자열 - \n와 같은 애들임

@문자열 표기법 - 이스케이프 문자에 대한 처리를 하지 않음!!!


배열
Sort - 정렬
Clear - 초기화
Clone - 똑같은 배열을 만들어준다
indexOf - 찾는값의 인덱스 나온다


const - 상수, 자동으로 내부에서 static으로 선언됨 반드시 초기화 되어야 한다
static - static이라는 특수한 영역에 메모리 생성, 클래스 전체에서 공유, 객체 생성이 없이도 접근이 가능합니다!!!

readonly 상수 - static 키워드 명시 않으면 static이 아닌 일반 상수, static 키워드 사용하지 않은 경우 초기화 하지 않아도 됨
readonly int asd = 0


문자열에서 숫자형으로의 변환
Convert.... Parse.... 등 메소드를 이용하는 방법
ToString(), ToInt32().....

대입연산자에서 myInt <<= 2 -> myInt = myInt<<2와 같음

반복문에서는 foreach를 많이 사용함


class - 객체 지향 프로그램의 핵심
class ClassName {
	//클래스 멤버
}

